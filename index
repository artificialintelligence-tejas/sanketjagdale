<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sanket Tanaji Jagdale - A Financial Journey</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- GSAP (GreenSock Animation Platform) & ScrollTrigger Plugin -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

    <!-- Three.js for 3D Background -->
    <script type="importmap">
        {
            "imports": {
                "three": "https://cdn.jsdelivr.net/npm/three@0.157.0/build/three.module.js",
                "three/addons/": "https://cdn.jsdelivr.net/npm/three@0.157.0/examples/jsm/"
            }
        }
    </script>

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0a0a0a;
            color: #f0f2f5;
            overflow-x: hidden;
            /* Add perspective for 3D transforms */
            perspective: 1000px;
        }

        #canvas-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
        
        .content-section {
            min-height: 100vh;
            width: 100%;
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }
        
        .hero-section {
            display: flex;
            flex-direction: column;
            text-align: center;
        }

        .scroll-down-indicator {
            position: absolute;
            bottom: 2rem;
            left: 50%;
            transform: translateX(-50%);
            color: rgba(255, 255, 255, 0.5);
            font-weight: 300;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            font-size: 0.75rem;
        }
        .scroll-down-indicator span {
            display: block;
            margin-top: 0.5rem;
            font-size: 1.5rem;
            animation: bounce 2s infinite;
        }
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-15px); }
            60% { transform: translateY(-5px); }
        }

        .timeline-container {
            width: 100%;
            max-width: 48rem; /* 768px */
        }

        .timeline-card {
            background-color: rgba(22, 27, 34, 0.65);
            backdrop-filter: blur(12px) saturate(180%);
            border-radius: 1.25rem;
            padding: 2.5rem;
            opacity: 0;
            transform: translateY(50px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: transform 0.4s ease, box-shadow 0.4s ease;
        }
        .timeline-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
        }

        .skill-cloud {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
            max-width: 40rem;
        }

        .skill-tag {
            background: linear-gradient(145deg, #4f46e5, #312e81);
            color: #e0e7ff;
            padding: 0.75rem 1.5rem;
            border-radius: 9999px;
            font-weight: 600;
            opacity: 0;
            border: 1px solid rgba(165, 180, 252, 0.3);
            transform: scale(0.5);
            transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275), 
                        box-shadow 0.4s ease, 
                        border-color 0.4s ease;
            transform-style: preserve-3d;
        }

        .skill-tag:hover {
            transform: scale(1.1) translateY(-5px) rotateX(10deg);
            box-shadow: 0 20px 35px rgba(79, 70, 229, 0.4);
            border-color: rgba(199, 210, 254, 0.7);
        }

        .btn {
            display: inline-block;
            font-weight: 600;
            padding: 0.875rem 2.5rem;
            border-radius: 0.5rem;
            text-align: center;
            transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275), 
                        box-shadow 0.4s ease, 
                        background-color 0.3s ease;
            transform-style: preserve-3d;
        }
        .btn-primary {
            background-color: #4f46e5;
            color: white;
        }
        .btn-primary:hover {
            background-color: #4338ca;
            transform: translateY(-4px) scale(1.05) rotateX(5deg);
            box-shadow: 0 15px 25px rgba(79, 70, 229, 0.3);
        }
        .btn-outline {
            background-color: transparent;
            border: 1px solid #4f46e5;
            color: #a5b4fc;
        }
        .btn-outline:hover {
            background-color: rgba(79, 70, 229, 0.1);
            transform: translateY(-4px) scale(1.05) rotateX(5deg);
            box-shadow: 0 0 25px rgba(79, 70, 229, 0.25);
        }

    </style>
</head>
<body>

    <div id="canvas-container"></div>

    <main id="scrolly-container">
        <!-- Section 1: Hero -->
        <section class="content-section hero-section" id="hero">
            <div>
                <h1 id="hero-title" class="text-5xl md:text-8xl font-extrabold tracking-tighter">Sanket Tanaji Jagdale</h1>
                <p id="hero-subtitle" class="mt-6 text-lg md:text-2xl text-indigo-300 max-w-3xl mx-auto opacity-0">
                    A diligent Assistant Accountant on a journey of financial excellence.
                </p>
            </div>
            <div class="scroll-down-indicator">
                Scroll to begin the journey
                <span>&darr;</span>
            </div>
        </section>

        <!-- Section 2: The Foundation (Education) -->
        <section class="content-section" id="education">
            <div class="timeline-container">
                <div class="timeline-card">
                    <h2 class="text-4xl font-bold text-indigo-300 mb-2">The Foundation</h2>
                    <p class="text-lg text-gray-400 mb-8">Building a strong base for a career in finance.</p>
                    <div class="space-y-6">
                        <div>
                            <h3 class="text-xl font-semibold">PG-MBA, 1st Year</h3>
                            <p class="text-gray-400">Parul University, Gujarat</p>
                        </div>
                        <div>
                            <h3 class="text-xl font-semibold">Bachelor of Commerce</h3>
                            <p class="text-gray-400">Shivaji University (2021 - 2024)</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Section 3: Professional Experience -->
        <section class="content-section" id="experience">
            <div class="timeline-container">
                <div class="timeline-card">
                    <h2 class="text-4xl font-bold text-indigo-300 mb-2">The First Step</h2>
                    <p class="text-lg text-gray-400 mb-8">Applying knowledge and gaining practical skills.</p>
                    <div>
                        <p class="text-sm text-gray-500 mb-1">Dec 2023 - Jul 2024</p>
                        <h3 class="text-xl font-semibold">Assistant Accountant</h3>
                        <p class="text-md text-gray-400 mb-4">Income tax and GST consultation, Palus</p>
                        <ul class="list-disc list-inside text-gray-400 space-y-2">
                            <li>Ensured high accuracy and compliance, leading to clean audits.</li>
                            <li>Identified and implemented cost-saving efficiencies.</li>
                            <li>Managed financial records and reconciled accounts.</li>
                        </ul>
                    </div>
                </div>
            </div>
        </section>

        <!-- Section 4: Core Competencies (Skills) -->
        <section class="content-section" id="skills">
            <div class="text-center">
                 <h2 class="text-4xl font-bold text-indigo-300 mb-12">Core Competencies</h2>
                 <div class="skill-cloud">
                    <div class="skill-tag">MS Excel</div>
                    <div class="skill-tag">Tally Prime</div>
                    <div class="skill-tag">SAP FICO (S4 HANA)</div>
                    <div class="skill-tag">Bookkeeping</div>
                    <div class="skill-tag">Financial Reporting</div>
                    <div class="skill-tag">Income Tax</div>
                    <div class="skill-tag">GST Consultation</div>
                 </div>
            </div>
        </section>

        <!-- Section 5: Strengths -->
        <section class="content-section" id="strengths">
            <div class="timeline-container">
                <div class="timeline-card">
                    <h2 class="text-4xl font-bold text-indigo-300 mb-8">Key Strengths</h2>
                    <div class="space-y-6">
                        <div>
                            <h3 class="text-xl font-semibold">Teamwork</h3>
                            <p class="text-gray-400">Establishing common goals and objectives to create a sense of purpose and unity within a team, ensuring everyone can work together towards achieving a shared mission.</p>
                        </div>
                        <div>
                            <h3 class="text-xl font-semibold">Self Learner</h3>
                            <p class="text-gray-400">Ability to identify and access a wide range of learning resources, including online courses, tutorials, and communities to continuously expand knowledge and skills independently.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Section 6: Community Involvement -->
        <section class="content-section" id="volunteering">
             <div class="timeline-container">
                <div class="timeline-card">
                    <h2 class="text-4xl font-bold text-indigo-300 mb-2">Community Involvement</h2>
                    <p class="text-lg text-gray-400 mb-8">Contributing to society while gaining practical experience and leadership skills.</p>
                    <div>
                        <p class="text-sm text-gray-500 mb-1">June 2021 - June 2022</p>
                        <h3 class="text-xl font-semibold">NSS Volunteer</h3>
                        <p class="text-md text-gray-400">National Service Scheme</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Section 7: The Future -->
        <section class="content-section" id="future">
            <div class="text-center max-w-3xl">
                <h2 class="text-5xl font-bold mb-6">The Journey Ahead</h2>
                <p class="text-xl text-gray-400 mb-8">
                    Continuously learning and evolving to meet new challenges in the world of finance. The pursuit of an MBA is the next chapter in a story of growth and ambition.
                </p>
                <div class="flex flex-col sm:flex-row justify-center items-center gap-4 mb-6">
                    <a href="mailto:sanketjagdale2377@gmail.com" class="btn btn-primary w-full sm:w-auto">
                        Connect via Email
                    </a>
                    <a href="https://drive.google.com/file/d/1C7pcsbJ9DliCYORBosQ98UI3yHG4gXAw/view?usp=sharing" download target="_blank" class="btn btn-outline w-full sm:w-auto">
                        Download Resume
                    </a>
                </div>
                <a href="tel:8623821142" class="inline-flex items-center text-indigo-300 hover:text-white transition-colors duration-300">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mr-2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.63A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
                    8623821142
                </a>
            </div>
        </section>

    </main>

    <script type="module">
        import * as THREE from 'three';

        // Wait for the DOM to be fully loaded before running any script
        document.addEventListener('DOMContentLoaded', () => {
            // --- THREE.JS 3D BACKGROUND ---
            let camera, scene, renderer;
            let particles;

            function init3D() {
                const container = document.getElementById('canvas-container');

                // Camera
                camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 1, 1000);
                camera.position.z = 500;

                // Scene
                scene = new THREE.Scene();

                // Particles
                const particleCount = 5000;
                const positions = new Float32Array(particleCount * 3);
                for (let i = 0; i < particleCount * 3; i++) {
                    positions[i] = (Math.random() - 0.5) * 2000;
                }
                
                const geometry = new THREE.BufferGeometry();
                geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
                
                const material = new THREE.PointsMaterial({
                    size: 1.5,
                    color: 0x4f46e5, // Indigo color
                    transparent: true,
                    opacity: 0.7
                });

                particles = new THREE.Points(geometry, material);
                scene.add(particles);

                // Renderer
                renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
                renderer.setPixelRatio(window.devicePixelRatio);
                renderer.setSize(window.innerWidth, window.innerHeight);
                container.appendChild(renderer.domElement);

                // Event Listeners
                window.addEventListener('resize', onWindowResize);
                document.addEventListener('mousemove', onMouseMove);
            }

            function onWindowResize() {
                if (!camera || !renderer) return;
                camera.aspect = window.innerWidth / window.innerHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(window.innerWidth, window.innerHeight);
            }

            let mouseX = 0, mouseY = 0;
            function onMouseMove(event) {
                mouseX = (event.clientX - window.innerWidth / 2) / 10;
                mouseY = (event.clientY - window.innerHeight / 2) / 10;
            }

            function animate3D() {
                requestAnimationFrame(animate3D);

                if (!camera || !scene || !renderer || !particles) return;

                const time = Date.now() * 0.00005;
                
                // Animate camera based on mouse movement
                camera.position.x += (mouseX - camera.position.x) * 0.05;
                camera.position.y += (-mouseY - camera.position.y) * 0.05;
                camera.lookAt(scene.position);

                // Animate particles
                particles.rotation.x = time * 0.2;
                particles.rotation.y = time * 0.4;
                
                renderer.render(scene, camera);
            }

            init3D();
            animate3D();


            // --- GSAP SCROLLYTELLING ANIMATIONS ---
            gsap.registerPlugin(ScrollTrigger);

            // Hero Section Animation
            gsap.to("#hero-subtitle", {
                opacity: 1,
                duration: 1.5,
                delay: 0.5
            });

            // Pin the hero section and fade out
            gsap.to("#hero", {
                scrollTrigger: {
                    trigger: "#hero",
                    start: "top top",
                    end: "bottom top",
                    scrub: true,
                    pin: true,
                },
                opacity: 0,
                scale: 0.8
            });

            // Animate cards on scroll
            const cards = gsap.utils.toArray('.timeline-card');
            cards.forEach(card => {
                gsap.to(card, {
                    scrollTrigger: {
                        trigger: card,
                        start: "top 80%",
                        toggleActions: "play none none none"
                    },
                    opacity: 1,
                    y: 0,
                    duration: 1,
                    ease: "power3.out"
                });
            });

            // Animate skill cloud
            gsap.to(".skill-tag", {
                scrollTrigger: {
                    trigger: "#skills",
                    start: "top 60%",
                    toggleActions: "play none none none"
                },
                opacity: 1,
                scale: 1,
                duration: 0.5,
                stagger: 0.1,
                ease: "back.out(1.7)"
            });

            // Animate 3D background with scroll
            ScrollTrigger.create({
                trigger: "#scrolly-container",
                start: "top top",
                end: "bottom bottom",
                scrub: 1,
                onUpdate: self => {
                    // Move camera closer on scroll
                    if(camera) {
                        camera.position.z = 500 - (self.progress * 400);
                    }
                    // Change particle color on scroll
                    if(particles) {
                        particles.material.color.setHSL(0.65, 0.7, 0.5 + self.progress * 0.3);
                    }
                }
            });
        });
    </script>
</body>
</html>
